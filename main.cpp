#include "ashu.cpp"

int main()
{
    //::SendMessage(::GetConsoleWindow(), WM_SYSKEYDOWN, VK_RETURN, 0x20000000);
    admin a;
    a.startPage();
    getche();
    return 0;
}

///to view this program in full screen mode :
                            ///change the console font size to 24
                            ///remove the comment from send message line ie line no :5
